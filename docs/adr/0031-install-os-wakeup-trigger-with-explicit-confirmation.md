# Install OS wakeup trigger with explicit confirmation

AgentScheduler will install and manage the operating-system wakeup trigger automatically only after explicit user confirmation. The VS Code extension will offer an Enable local scheduling flow, and the CLI will expose install and uninstall commands so the background scheduling mechanism remains transparent and debuggable.
