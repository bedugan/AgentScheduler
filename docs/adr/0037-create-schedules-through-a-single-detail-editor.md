# Create schedules through a single detail editor

AgentScheduler will create schedules as draft schedules in a Codex-like schedule detail editor instead of using a multi-step wizard. We chose this because the primary users need to edit instructions, cadence, harness mode, approval mode, model, and context together, and sensible defaults allow quick creation without forcing premature choices.
