# Keep draft schedules disabled until enabled

Draft schedules will not start automatic recurrence until the user explicitly enables them. We chose this because schedule creation should allow editing, validation, and manual test runs without accidentally starting background agent runs.
