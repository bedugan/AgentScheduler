# Count manual runs against run caps

Manual runs will count against scheduler-native run caps such as "run X times." AgentScheduler will show a simple run counter, such as completed runs out of total, so users can see why a looping schedule continues or stops.
