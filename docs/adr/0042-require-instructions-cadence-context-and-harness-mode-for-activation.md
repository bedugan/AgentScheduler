# Require instructions, cadence, context, and harness mode for activation

AgentScheduler will require run instructions, run cadence, target context, and harness mode before a natural-language schedule can become active after confirmation. Approval mode and model can use defaults; if any required activation field is missing, the schedule is created as a disabled draft for review.
