# Enable complete natural-language schedules after one confirmation

When a natural-language schedule request is complete and explicit, AgentScheduler will create an active schedule after one user confirmation. If required details are missing or the request is risky, AgentScheduler will create a disabled draft instead so the user can review and enable it manually.
