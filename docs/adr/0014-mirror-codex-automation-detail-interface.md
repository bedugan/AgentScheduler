# Mirror the Codex automation detail interface

AgentScheduler's editor control surface will use a Codex-like schedule detail view as its primary interface pattern. We chose this because the target experience is scheduling and reviewing local agent runs, and Codex already exposes the relevant concepts together: instructions, status, next run, last run, run location, project or context, cadence, model, and previous runs.
