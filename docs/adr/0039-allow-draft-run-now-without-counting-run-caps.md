# Allow draft Run now without counting run caps

Draft schedules will allow Run now so users can validate the current schedule revision before enabling recurrence. Draft runs will not increment scheduler-native run caps because they are pre-enable validation runs, while manual runs on enabled schedules continue to count against caps.
