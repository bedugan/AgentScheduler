# Default target context to current VS Code workspace

When a user creates a schedule from natural language inside an open VS Code workspace, AgentScheduler will default the target context to the current workspace unless the user specifies another project or path. If no workspace is open and no target context is supplied, the schedule will be created as a disabled draft.
