# Use Copilot approval modes as primary policy presets

AgentScheduler will use Copilot's visible VS Code approval modes as the primary permission presets for Copilot schedules: Default Approvals, Bypass Approvals, and Autopilot. We chose this because users should recognize the same approval vocabulary they already see in VS Code, while advanced policy mapping can remain available for harness-specific details.
