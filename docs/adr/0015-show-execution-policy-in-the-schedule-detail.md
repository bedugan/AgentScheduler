# Show execution policy in the schedule detail

AgentScheduler will expose execution policy as first-class controls in the schedule detail, alongside run instructions, cadence, context, harness mode, model, and run history. We chose this because unattended runs must make their permissions and approval behavior visible before execution, not hide them in global settings.
