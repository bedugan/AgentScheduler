# Allow manual runs when local scheduling is disabled

AgentScheduler will allow manual runs when local scheduling is disabled if the selected harness can run from the current extension or session. Local scheduling disabled means automatic OS wakeups are unavailable; it does not prevent user-initiated runs that still pass harness preflight and execution policy.
