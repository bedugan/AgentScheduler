# Default new schedules to Default Approvals

New Copilot schedules will default to Default Approvals. We chose this because it matches the visible VS Code permission default, respects the user's configured Copilot settings, and lets the selected harness mode resolve the approval mode into concrete backend switches such as Copilot CLI tool permissions and sandbox settings at run time.
